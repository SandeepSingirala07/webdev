import { MessageConstantsDirective } from './message-constants.directive';

describe('MessageConstantsDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageConstantsDirective();
    expect(directive).toBeTruthy();
  });
});
